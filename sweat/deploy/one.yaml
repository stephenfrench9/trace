apiVersion: v1
kind: Service
metadata:
  name: app-one
  labels:
    app: ape-one
spec:
  type: LoadBalancer
  ports:
  - port: 5000
  selector:
    app: ape-one
---
apiVersion: v1
kind: Service
metadata:
  name: trace-one
spec:
  ports:
    - port: 8000
      targetPort: 8000
      name: zero
    - port: 8081
      targetPort: 8081
      name: one
    - port: 8082
      targetPort: 8082
      name: two
    - port: 8083
      targetPort: 8083
      name: three
  selector:
    app: ape-one
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: one
  labels:
    app: ape-one
  annotations:
    "sidecar.jaegertracing.io/inject": "true"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ape-one
  template:
    metadata:
      labels:
        app: ape-one
    spec:
      containers:
      - name: ape-one
        image: stephenfrench9/zserv:demo
        ports:
        - containerPort: 5000
        - containerPort: 8000
        - containerPort: 8001
        - containerPort: 8002
        - containerPort: 8003
